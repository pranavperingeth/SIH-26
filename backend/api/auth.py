"""
Auth placeholder for ChainTrace (Person E).

JWT authentication and RBAC are Phase 4 scope for Person E and are NOT
implemented here. This file exists so the module path is stable for
future phases and so other files can `from .auth import ...` without
breaking once real auth is added.
"""

# Phase 4 TODO: implement JWT token issuance/verification and
# role-based access control (ADMIN, INVESTIGATOR, ANALYST, VIEWER).
